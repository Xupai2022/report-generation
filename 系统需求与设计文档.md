# MSS AI PPT 安全报告生成系统 - 系统需求与设计文档

**版本**: 1.0
**日期**: 2026-01-30
**系统版本**: V2 (AI驱动模式)

---

## 1. 系统概述

### 1.1 系统定位

MSS AI PPT 是一个**安全托管服务(MSS)月度报告自动生成平台**,通过结合 **AI智能生成** 与 **数据驱动填充** 两种模式,将结构化安全数据(JSON格式)转换为专业PowerPoint报告。

**核心能力**:
- 支持从Excel文件提取数据并转换为JSON格式
- 基于V2模板架构,支持占位符驱动的内容生成
- 数据占位符(ai_generate=false):直接从输入数据提取,填充数字、文本、图表、表格
- AI占位符(ai_generate=true):调用OpenAI兼容API生成威胁分析、整改建议等洞察性内容
- 原生PowerPoint图表/表格渲染,支持用户在PPT中二次编辑
- FastAPI后端 + 静态HTML前端(无构建步骤)
- **异步任务队列**:支持后台处理、进度追踪、自动重试
- **RESTful API设计**:统一的 `/api/v1/*` 路由结构

### 1.2 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│  数据源层                                                    │
│  - Excel文件(openpyxl提取) / JSON文件                       │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│  并发安全层                                                  │
│  - 会话隔离(Session Manager): 唯一session_id               │
│  - 文件锁(FileLock): 跨平台文件锁,防止并发冲突            │
│  - LLM并发限流(Semaphore): 最大3个并发AI请求               │
│  - IP速率限制: 30秒/请求 (防止滥用)                         │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│  任务管理层 (JobManager)                                     │
│  - JobStore: 任务状态持久化到outputs/jobs/                  │
│  - 任务状态: pending → processing → completed/failed        │
│  - 自动重试: 指数退避,最多3次                               │
│  - 任务清理: 保留7天后自动删除                              │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│  报告生成服务层 (ReportService)                             │
│  ├─ LLMOrchestratorV2: AI内容生成(支持智能分批)             │
│  ├─ PPTGeneratorV2: 图表/表格渲染(9种图表类型)              │
│  ├─ ValidatorV2: 关键数字校验                               │
│  └─ PreviewGenerator: PPTX→PDF→PNG预览图(自动清理7天)       │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│  API层 (FastAPI) - RESTful v1                               │
│  - POST   /api/v1/reports: 创建报告(异步,返回job_id)        │
│  - GET    /api/v1/jobs/{job_id}: 查询任务状态               │
│  - GET    /api/v1/reports/{job_id}: 下载PPTX报告            │
│  - POST   /api/v1/sessions/upload: 上传Excel并解析          │
│  - POST   /api/v1/reports/batch: 批量重写幻灯片             │
│  - GET    /api/v1/sessions/{session_id}/preview: 生成预览图 │
│  - GET    /api/v1/system/health: 健康检查(含LLM并发状态)    │
│  - WebSocket /ws/{client_id}: 实时进度推送                  │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│  输出层                                                      │
│  - PPTX报告文件 (outputs/sessions/{session_id}/)            │
│  - SlideSpec JSON (中间数据, 支持rewrite)                   │
│  - 预览图 PNG (outputs/previews/{job_id}/, 自动清理7天)     │
│  - 应用日志 (outputs/logs/app.log, 10MB轮转 x 5)            │
│  - 审计日志 (outputs/logs/audit.jsonl)                      │
│  - 任务状态 (outputs/jobs/states/{job_id}.json)             │
│  - 任务索引 (outputs/jobs/index.json)                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 功能性需求

### 2.1 报告生成功能

#### 2.1.1 功能描述

**用户视角**:
安全运营人员/分析师通过Web界面或API,上传原始数据(Excel/JSON),选择报告模板(管理层/技术层),系统自动生成包含数据分析与AI洞察的PowerPoint报告。

**系统行为**:
1. 接收输入数据(input_id)和模板ID(template_id)
2. 提取非AI占位符:从输入数据中按照source路径提取文本、数字、图表数据、表格数据
3. 生成AI占位符:调用OpenAI兼容LLM,根据ai_instruction生成威胁总结、趋势分析、整改建议等内容
4. 数字校验:验证AI生成内容中引用的关键数字与输入数据一致
5. 渲染PPT:将所有占位符内容填充到模板,渲染图表/表格,保存PPTX文件
6. 生成预览图:可选生成PNG预览图供Web展示

#### 2.1.2 正常场景

**步骤**:
1. 用户调用 `POST /api/v1/reports`,提交input_id与template_id
2. 系统立即创建Job记录(job_id, status=pending),返回202 Accepted + job_id
3. 后台异步处理:
   - 状态更新为processing
   - 等待LLM槽位(Semaphore限流)
   - 提取数据 → 调用AI生成内容 → 数字校验 → 渲染PPT → 保存文件
   - WebSocket推送进度(0% → 100%)
   - 状态更新为completed/failed
4. 用户轮询 `GET /api/v1/jobs/{job_id}` 查询任务状态
5. 状态=completed后,调用 `GET /api/v1/reports/{job_id}` 下载PPTX

**期望**:
- 立即返回job_id,不阻塞客户端
- 生成PPTX文件存在,可用PowerPoint打开
- AI生成内容语言流畅,关键数字与输入数据一致
- 图表/表格为原生对象,支持编辑
- 总耗时<60秒(10张幻灯片)
- WebSocket实时推送进度,客户端可显示进度条

#### 2.1.3 异常场景

**场景A: AI API超时/限流**
- **步骤**: API响应超时或限流 → 系统重试4次(指数退避) → 失败后任务状态=failed → 触发自动重试(最多3次)
- **期望**: 用户通过GET /api/v1/jobs/{job_id}看到失败原因,系统自动重试,最终成功或提示切换mock模式

**场景B: AI响应格式错误**
- **步骤**: LLM返回的JSON无法解析 → 系统自动重试5次 → 失败后任务状态=failed → 触发自动重试
- **期望**: 用户收到明确错误提示"AI响应格式错误,已重试N次"

**场景C: 输入数据不完整**
- **步骤**: 输入JSON缺少必需字段 → 缺失数据用空值填充 → 报告仍能生成 → 任务状态=completed
- **期望**: PPTX文件已生成,缺失部分显示为空或占位文本,warnings字段提示数据缺失

**场景D: 并发文件冲突**
- **步骤**: 多个请求同时写入 → 文件锁等待60秒 → 超时则任务状态=failed
- **期望**: 不会出现文件损坏,失败任务自动重试

**场景E: 任务超时或中断**
- **步骤**: 服务器重启导致任务中断 → 下次启动时JobManager检测到processing状态的任务 → 自动重试
- **期望**: 任务恢复执行,用户无需手动重新提交

#### 2.1.4 约束条件

**可靠性约束**:
- AI生成重试次数:5次(parse error) + 4次(API error)
- 数字校验容差:±0.01(浮点数比较)
- 会话ID唯一性:uuid8(8位)+微秒时间戳,碰撞概率<1/4.3亿

**系统兼容性**:
- Python版本:3.9+ (依赖FastAPI, pydantic v2)
- 操作系统:Windows/Linux (文件锁跨平台兼容)
- LibreOffice依赖:需安装LibreOffice用于预览图生成(PPTX→PDF转换)
- OpenAI API兼容性:支持OpenAI官方API及兼容API(如GLM, Claude等,需设置OPENAI_BASE_URL)

**部署约束**:
- 单机部署:当前版本使用本地文件锁,不支持分布式多实例(需升级为Redis锁)
- 存储路径:outputs/目录需有写权限
- 环境变量:OPENAI_API_KEY必需(若ENABLE_LLM=true)

---

### 2.2 Excel数据导入功能

#### 2.2.1 功能描述

**用户视角**:
用户通过 `POST /api/v1/sessions/upload` 端点上传Excel文件(.xlsx格式),系统自动解析工作表数据,转换为标准JSON格式,并返回session_id供后续生成报告使用。

**系统行为**:
1. 接收Excel文件上传(Content-Type: multipart/form-data)
2. 安全校验:文件格式(.xlsx)、MIME类型、大小(<10MB)
3. 使用openpyxl只读模式解析Excel(data_only=True,不执行宏和公式)
4. 提取数据:
   - 基础信息(客户名称、报告周期等)
   - 统计数字(告警总数、各级别数量等)
   - 图表数据(柱状图/饼图数据源,格式为categories+values)
   - 表格数据(明细列表,转换为对象数组)
5. 生成唯一session_id,创建session目录
6. 保存解析后的JSON到session目录(`outputs/sessions/{session_id}/input.json`)
7. 返回session_id与数据预览

**实现状态**: 功能已实现,端点位于 `routers/v1/sessions.py`。

#### 2.2.2 正常场景

**步骤**:
1. 用户调用 `POST /api/v1/sessions/upload`,上传Excel文件
2. 系统校验格式 → 解析工作表 → 提取业务数据 → 生成JSON → 保存到session目录

**期望**:
- 返回200 OK + session_id与数据预览(客户名称、告警总数等)
- 响应时间<5秒(10MB文件)
- 可使用session_id调用 `POST /api/v1/reports` 生成报告

#### 2.2.3 异常场景

**场景A: 文件格式错误**
- **步骤**: 上传非.xlsx格式 → 系统校验MIME类型 → 返回HTTP 400错误
- **期望**: 用户收到"仅支持.xlsx格式"提示,转换文件后重新上传

**场景B: 文件大小超限**
- **步骤**: 文件>10MB → 系统拒绝接收 → 返回HTTP 413错误
- **期望**: 用户压缩数据或拆分文件后重新上传

**场景C: Excel结构不匹配**
- **步骤**: 缺少预期工作表或单元格为空 → 记录警告 → 缺失数据填充null → 继续解析
- **期望**: 报告仍能生成,缺失部分显示为空

**场景D: 并发上传冲突**
- **步骤**: 多个用户同时上传同名文件 → 每个请求生成唯一session_id
- **期望**: 所有用户上传互不干扰

#### 2.2.4 约束条件

**安全约束**:
- 仅接受`.xlsx`格式(Office Open XML,无宏)
- 拒绝`.xlsm`(启用宏的Excel)、`.xls`(旧格式)、`.xlsb`(二进制格式)
- MIME类型双重验证:检查Content-Type与文件magic bytes
- openpyxl只读模式:data_only=True,不执行公式或宏代码

**数据格式约束**:
- Excel中日期单元格必须为日期格式(或ISO 8601字符串),不支持"2025年12月"等文本
- 数字单元格必须为数字类型,不支持带单位的文本(如"100万")
- 列表数据(分类、明细)必须连续,不支持跳行

---

### 2.3 内容改写功能(Rewrite)

#### 2.3.1 功能描述

**用户视角**:
用户预览生成的报告后,发现某张幻灯片的AI生成内容不满意(如"威胁分析太空泛"、"建议不可落地"),可通过Web界面或API提交改写请求,系统重新生成该幻灯片的指定占位符内容。

**系统行为**:
1. 接收请求 `POST /api/v1/reports/batch`,提交job_id, slides数组(包含slide_key和new_content)
2. 加载已保存的SlideSpecV2 JSON文件
3. 更新指定幻灯片的占位符内容(合并new_content)
4. 重新渲染整个PPTX(保持其他幻灯片不变)
5. 保存更新后的SlideSpec与PPTX文件
6. 返回新的job_id与文件路径

**实现状态**: 功能已实现,端点位于 `routers/v1/reports.py`。

#### 2.3.2 正常场景

**步骤**:
1. 用户调用 `POST /api/v1/reports/batch`,提交job_id, slides数组
2. 系统加载SlideSpec → 批量更新占位符内容 → 重新渲染PPT → 保存文件

**期望**:
- PPTX文件已更新,指定幻灯片内容已替换
- 响应时间<5秒(无需调用LLM)
- SlideSpec JSON同步更新,支持后续再次改写

#### 2.3.3 异常场景

**场景A: job_id格式错误**
- **步骤**: 提交格式不正确的job_id → 系统校验失败 → 返回HTTP 400错误
- **期望**: 用户修正job_id格式(session_id:template_id)

**场景B: SlideSpec文件不存在**
- **步骤**: 指定session_id或template_id无对应SlideSpec → 返回HTTP 404错误
- **期望**: 用户先调用 `/generate` 生成报告

**场景C: slide_key不存在**
- **步骤**: 提交的slide_key在SlideSpec中找不到 → 更新无效 → PPT未变化
- **期望**: 用户检查slide_key拼写

#### 2.3.4 约束条件

- 仅支持V2模板的改写(V1模板不支持)
- new_content必须是字典类型,键为占位符token名称
- 改写操作不会触发AI重新生成,仅替换指定占位符的内容(用户需手动编写新内容或通过前端集成改写助手)

---

### 2.5 报告预览功能

#### 2.5.1 功能描述

**用户视角**:
用户生成报告后,可通过Web界面查看报告的PNG预览图(每张幻灯片一张图片),无需下载PPTX文件。

**系统行为**:
1. 接收 `GET /api/v1/sessions/{session_id}/preview?template_id={template_id}`
2. 查找对应的PPTX文件(若不存在且regenerate_if_missing=true,则从SlideSpec重新渲染)
3. 调用PPTPreviewGenerator:
   - 使用LibreOffice将PPTX转换为PDF:`soffice --headless --convert-to pdf`
   - 使用PyMuPDF(fitz)将PDF转换为PNG图片(每页一张)
4. 将PNG图片保存到 `outputs/previews/{sanitized_job_id}/slide*.png`
5. 自动清理超过7天的旧预览图(可配置PREVIEW_CLEANUP_DAYS)
6. 返回图片URL列表(相对路径,通过StaticFiles挂载访问)

**实现状态**: 功能已实现,代码位于 `modules/preview_generator.py`,端点位于 `routers/v1/sessions.py`

#### 2.5.2 正常场景

**步骤**:
1. 用户调用 `GET /api/v1/sessions/{session_id}/preview?template_id={template_id}`
2. 系统查找PPTX → LibreOffice转换为PDF → PyMuPDF渲染为PNG → 自动清理旧图 → 返回图片URL列表

**期望**:
- 用户可在浏览器中查看PNG图片
- 响应时间10-30秒(8张幻灯片)
- PNG图片质量清晰,分辨率1920x1080
- 旧预览图自动清理,节省磁盘空间

#### 2.5.3 异常场景

**场景A: LibreOffice未安装**
- **步骤**: 系统PATH中找不到soffice → 返回HTTP 500错误
- **期望**: 运维安装LibreOffice并添加到PATH

**场景B: PPTX文件不存在**
- **步骤**: 指定job_id的PPTX不存在 → 返回HTTP 404错误
- **期望**: 用户先调用 `/generate` 生成报告

**场景C: PDF转换失败**
- **步骤**: LibreOffice转换失败(如PPTX损坏) → 返回HTTP 500错误
- **期望**: 用户检查PPTX有效性或重新生成

#### 2.5.4 约束条件

- 预览图分辨率:默认1920x1080(可在代码中调整)
- PNG图片大小:约200-500KB/张(取决于幻灯片复杂度)
- 转换性能:8张幻灯片约10-30秒(LibreOffice转换为主要瓶颈)
- LibreOffice依赖:Windows需手动设置LIBREOFFICE_PATH环境变量(若未在默认路径)

---

## 3. 非功能性需求

### 3.1 性能分析

#### 3.1.1 接口响应时间

**基于当前代码实现与文档描述**:

| 端点 | 场景 | 预期响应时间 | 性能瓶颈 |
|------|------|-------------|---------|
| POST /generate | 简单报告(5张幻灯片,无AI) | 5-10秒 | PPTX渲染、文件I/O |
| POST /generate | 复杂报告(10张幻灯片,启用AI) | 30-60秒 | LLM API调用(20-40秒)、智能分批 |
| POST /upload-excel | 10MB Excel文件 | <5秒 | openpyxl解析、JSON序列化 |
| POST /rewrite | 单张幻灯片改写 | 3-5秒 | PPTX重新渲染 |
| GET /preview | 8张幻灯片 | 10-30秒 | LibreOffice PDF转换(8-20秒) |
| WebSocket /ws | 进度推送 | <100ms | 网络延迟 |

**性能设计要点**:
1. **智能分批(Smart Batching)**: LLMOrchestratorV2根据prompt token数估算,将幻灯片分成多批(每批<6000 tokens),避免单次API超时
2. **并发限流**: 使用asyncio.Semaphore限制最多3个并发LLM请求,防止API rate limit
3. **会话隔离**: 每个用户请求使用独立session目录,避免文件路径冲突导致的锁等待
4. **文件锁粒度**: 仅在文件写入时加锁(60秒超时),读取时使用30秒超时,减少阻塞

#### 3.1.2 并发与吞吐量

**并发测试结果(来自文档CONCURRENCY_FIX_REPORT.md)**:
- 测试场景:5个用户同时调用 `/generate` (无AI模式,仅数据填充)
- 结果:
  - 100%成功率(5/5)
  - 平均响应时间:0.76秒
  - 无会话ID碰撞
  - 无文件锁冲突

**并发限制分析**:
- **LLM并发限流**: MAX_CONCURRENT_LLM_REQUESTS=3,超过的请求会等待
- **文件I/O并发**: 通过session隔离,每个请求写入独立目录,理论支持无限并发
- **实际瓶颈**: OpenAI API rate limit(取决于API计划,如GPT-4限制10,000 tokens/min)

**吞吐量估算**:
- 无AI模式:约10-20 报告/分钟(单机,瓶颈为PPTX渲染与磁盘I/O)
- 启用AI模式:约3-6 报告/分钟(瓶颈为LLM API调用,受并发限流与API rate limit影响)

#### 3.1.3 压力测试建议

**测试场景设计**:
1. **基准测试**: 单用户生成100个报告(无AI),测量平均响应时间与成功率
2. **并发测试**: 10个用户同时生成报告(启用AI),观察LLM并发限流效果与响应时间
3. **峰值测试**: 20个用户同时上传Excel并生成报告,测试会话隔离与文件锁稳定性
4. **长时间运行测试**: 持续运行24小时,每分钟生成1个报告,观察内存泄漏与文件积累

**性能优化方向**:
- 使用异步I/O(aiofiles)替代同步文件操作
- 引入缓存(Redis)缓存SlideSpec JSON,减少重复读取
- 优化图表渲染:批量创建图表对象,减少python-pptx API调用次数
- 分布式部署:使用Redis分布式锁替代本地文件锁,支持多实例水平扩展

---

### 3.2 可靠性

#### 3.2.1 过载保护

**LLM并发限流(已实现)**:
- 机制:使用asyncio.Semaphore(MAX_CONCURRENT_LLM_REQUESTS=3)限制并发LLM请求
- 行为:第4个请求会等待,直到前3个请求中有一个完成
- 监控:通过 `GET /health` 查看当前并发状态:
```json
{
  "llm_concurrency": {
    "max_concurrent_requests": 3,
    "available_slots": 1,
    "active_requests": 2,
    "is_saturated": false
  }
}
```
- 保障能力:防止OpenAI API rate limit导致的全局请求失败

**文件锁超时保护(已实现)**:
- 机制:使用FileLock,设置超时时间(60秒写入、30秒读取)
- 行为:若无法在超时内获取锁,抛出TimeoutError,请求失败
- 保障能力:避免死锁,确保文件操作原子性

**当前代码未体现的保护机制**:
- API rate limit自动退避:当前仅在RateLimitError时重试,未实现指数退避(建议添加)
- 请求队列长度限制:FastAPI默认无队列长度限制,可能导致内存耗尽(建议配置Uvicorn --backlog参数)

#### 3.2.2 高可用与容灾

**当前架构限制(单机部署)**:
- 无法跨实例共享session状态(文件锁为本地锁)
- 若服务器宕机,正在生成的报告会丢失(未持久化到数据库)
- 无健康检查端点自动重启机制

**建议的高可用设计(未实现)**:
1. **负载均衡**: 使用Nginx/HAProxy分发请求到多个FastAPI实例
2. **共享存储**: 将outputs/目录挂载到NFS/S3,所有实例共享
3. **分布式锁**: 使用Redis实现分布式文件锁,替代本地FileLock
4. **任务队列**: 使用Celery/RQ将报告生成任务异步化,支持失败重试
5. **数据库持久化**: 将SlideSpec与job状态存储到PostgreSQL/MongoDB,支持断点续传

**故障恢复能力**:
- **进程崩溃**: 残留文件锁(*.lock)需手动清理,建议启动时自动清理超过5分钟的陈旧锁
- **OpenAI API故障**: 支持降级到mock模式(use_mock=true),填充占位内容
- **LibreOffice崩溃**: 预览生成失败不影响PPTX文件,用户仍可下载报告

#### 3.2.3 数据持久性

**当前实现**:
- PPTX文件:存储在本地文件系统 `outputs/sessions/{session_id}/`
- SlideSpec JSON:同上,支持改写时恢复
- 审计日志:追加写入 `outputs/logs/audit.jsonl`

**数据丢失风险**:
- 磁盘故障导致outputs/目录丢失
- 24小时自动清理会话目录后,历史报告无法访问

**建议增强(未实现)**:
- 实时备份outputs/目录到对象存储(如AWS S3, MinIO)
- 审计日志写入ElasticSearch/Loki,支持集中查询
- 关键数据库化:job状态、SlideSpec存储到PostgreSQL,支持查询与统计

#### 3.2.4 SLA指标

**基于当前代码,推荐的SLA指标**:
- **服务可用度**: 99.5%(月允许停机时间约3.6小时,不考虑OpenAI API故障)
- **报告生成成功率**: 95%(启用AI模式,考虑5%的LLM API失败率)
- **数据持久性**: 99.9%(依赖文件系统可靠性,建议RAID或云存储)
- **响应时间P95**: <90秒(启用AI模式,10张幻灯片)

---

### 3.3 可维护性

#### 3.3.1 日志与可观测性

**当前日志体系**:
1. **应用日志**:
   - 格式:`%(asctime)s - %(name)s - %(levelname)s - [%(request_id)s] - %(message)s`
   - 输出:
     - 控制台(StreamHandler)
     - 文件(RotatingFileHandler): `outputs/logs/app.log`
   - 轮转策略:10MB x 5个备份文件
   - 日志级别:INFO(可通过LOG_LEVEL环境变量配置)
   - 关键日志点:
     - LLM调用:`logger.info("🎯 LLM generation attempt {attempt}/{max_retries}")`
     - 批次进度:`logger.info("🔄 Processing batch {i+1}/{total_batches}")`
     - 图表渲染:`logger.info(f"Updated P11_bar chart with {len(categories)} categories")`
     - 任务状态:`logger.info(f"✓ Job {job_id} completed successfully")`

2. **审计日志**:
   - 格式:JSONL(每行一个JSON对象)
   - 路径:`outputs/logs/audit.jsonl`
   - 记录事件:
     - generate_v2: 报告生成完成
     - validation_warning_v2: 数字校验警告
     - rewrite: 内容改写操作
     - job_created: 任务创建
     - job_completed: 任务完成
     - job_failed: 任务失败
   - 示例:
```json
{
  "timestamp": "2026-01-30T14:30:25.123456",
  "event": "generate_v2",
  "job_id": "a3f2c5d8_20250129143025:mss_executive_v2",
  "severity": "info",
  "details": {"template_id": "mss_executive_v2", "slides_count": 8},
  "request_id": "req_abc123"
}
```

3. **中间件日志**:
   - `RequestIdMiddleware`: 为每个请求分配唯一ID,记录请求开始/结束
   - `ErrorLoggingMiddleware`: 捕获所有未处理异常,记录详细堆栈信息

**日志增强(相比文档初版)**:
- ✅ 应用日志持久化到文件(10MB轮转 x 5)
- ✅ 请求ID追踪(跨模块关联日志)
- ✅ 结构化错误日志
- ❌ 无Prometheus metrics端点
- ❌ 无分布式追踪(OpenTelemetry集成)

**建议增强**:
- 集成structlog,输出结构化JSON日志
- 添加 `GET /metrics` 端点,暴露Prometheus指标
- 关键路径添加trace_id,支持跨服务追踪

#### 3.3.2 监控与告警

**可监控的关键指标(基于当前代码)**:
1. **服务健康**: `GET /api/v1/system/health` 返回200 OK
2. **LLM并发状态**: `GET /api/v1/system/health` 中的llm_concurrency字段
3. **任务队列**: `GET /api/v1/jobs` 统计pending/processing/completed/failed数量
4. **会话目录数量**: `ls outputs/sessions/ | wc -l`(过多表示清理机制失效)
5. **任务状态目录**: `ls outputs/jobs/states/ | wc -l`(监控任务积压)
6. **审计日志大小**: `du -h outputs/logs/audit.jsonl`(过大需轮转)
7. **应用日志轮转**: 检查`outputs/logs/app.log.*`备份数量
8. **磁盘空间**: `df -h outputs/`(建议告警阈值80%)

**建议的告警规则**:
- LLM并发饱和度>80%(持续5分钟) → 告警:需扩容或优化prompt
- 报告生成失败率>10%(1小时) → 告警:检查OpenAI API状态
- processing状态任务数>10(持续10分钟) → 告警:任务处理异常
- 会话目录数>1000(24小时未清理) → 告警:清理任务失效
- 磁盘空间>90% → 紧急告警:清理历史数据或扩容

#### 3.3.3 故障定位能力

**基于当前日志,故障定位流程**:
1. **用户报告"生成失败"**:
   - 查看任务状态:`GET /api/v1/jobs/{job_id}`
   - 查看应用日志:`grep "{job_id}" outputs/logs/app.log`
   - 若是LLM错误,查看重试日志:`grep "LLM generation attempt" app.log`
   - 使用request_id关联所有相关日志

2. **用户报告"内容不准确"**:
   - 查看审计日志:`grep "validation_warning_v2" outputs/logs/audit.jsonl`
   - 检查数字校验警告,定位AI生成的错误数据

3. **系统响应慢**:
   - 检查LLM并发状态:`GET /api/v1/system/health`,若is_saturated=true则为并发瓶颈
   - 查看批次日志:`grep "Batch sizing" app.log | grep "base="`(检查prompt token数是否过大)
   - 查看任务队列积压:`GET /api/v1/jobs?status=processing`

4. **文件锁超时**:
   - 搜索日志:`grep "FileLock" app.log | grep "timeout"`
   - 检查残留锁文件:`find outputs/ -name "*.lock" -mmin +5`(超过5分钟的锁为陈旧锁)

5. **任务自动重试未恢复**:
   - 查看任务重试历史:`GET /api/v1/jobs/{job_id}`(查看retry_count字段)
   - 检查错误信息是否为不可恢复错误(如输入数据不存在)

**调试友好性**:
- 代码中广泛使用logger.info/warning/error,关键步骤有日志覆盖
- 异常消息包含上下文(如job_id, session_id, template_id, request_id)
- SlideSpec JSON可直接查看中间数据,支持离线调试
- 任务状态持久化到JSON文件,服务重启后可恢复

#### 3.3.4 扩容与运维

**水平扩容限制(当前架构)**:
- 不支持多实例部署(文件锁为本地锁)
- 需改造:引入Redis分布式锁,共享存储

**垂直扩容**:
- 增加MAX_CONCURRENT_LLM_REQUESTS(需确认OpenAI API plan支持)
- 增加CPU核心数(FastAPI使用Uvicorn,支持多worker)

**运维操作**:
1. **定期清理**: 调用 `DELETE /api/v1/sessions?max_age_hours=24`,清理过期session
2. **日志轮转**: 已自动轮转(10MB x 5),无需手动配置
3. **任务清理**: 启动时自动清理超过7天的任务(可通过JOB_RETENTION_DAYS配置)
4. **陈旧锁清理**: 启动时自动清理超过5分钟的陈旧锁
5. **健康检查**: 配置监控系统定期调用 `GET /api/v1/system/health`
6. **备份**: 定期备份outputs/sessions/目录到对象存储

---

### 3.4 开放性

#### 3.4.1 API接口能力

**当前提供的REST API(基于app.py)**:

**报告管理 (`/api/v1/reports`)**:
| 端点 | 方法 | 用途 | 权限控制 |
|------|------|------|---------|
| /api/v1/reports | POST | 创建报告生成任务(异步,返回job_id) | IP速率限制(30s/次) |
| /api/v1/reports/{job_id} | GET | 下载PPTX报告文件 | 无 |
| /api/v1/reports/batch | POST | 批量重写幻灯片内容 | 无 |

**任务管理 (`/api/v1/jobs`)**:
| 端点 | 方法 | 用途 | 权限控制 |
|------|------|------|---------|
| /api/v1/jobs/{job_id} | GET | 查询任务状态和进度 | 无 |
| /api/v1/jobs | GET | 列出所有任务(支持分页和筛选) | 无 |

**会话管理 (`/api/v1/sessions`)**:
| 端点 | 方法 | 用途 | 权限控制 |
|------|------|------|---------|
| /api/v1/sessions/upload | POST | 上传Excel文件并解析 | 无 |
| /api/v1/sessions/{session_id}/preview | GET | 生成报告预览图 | 无 |
| /api/v1/sessions | DELETE | 清理旧会话(支持max_age_hours参数) | 无(建议限制为管理员) |

**模板与输入 (`/api/v1/templates`, `/api/v1/inputs`)**:
| 端点 | 方法 | 用途 | 权限控制 |
|------|------|------|---------|
| /api/v1/templates | GET | 列出可用模板 | 无 |
| /api/v1/templates/{template_id} | GET | 获取模板详情 | 无 |
| /api/v1/inputs | GET | 列出可用输入数据集 | 无 |
| /api/v1/inputs/{input_id} | GET | 获取输入数据详情 | 无 |

**系统管理 (`/api/v1/system`)**:
| 端点 | 方法 | 用途 | 权限控制 |
|------|------|------|---------|
| /api/v1/system/health | GET | 健康检查(含LLM并发状态、磁盘空间) | 无(公开) |

**实时通信**:
| 端点 | 协议 | 用途 | 权限控制 |
|------|------|------|---------|
| /ws/{client_id} | WebSocket | 实时进度推送 | 无 |

**OpenAPI文档**:
- FastAPI自动生成OpenAPI 3.0规范
- 访问路径:`GET /docs`(Swagger UI) 或 `GET /redoc`(ReDoc) 或 `GET /openapi.json`

**当前实现的安全机制**:
- IP速率限制:`POST /api/v1/reports` 限制30秒/请求(同IP)
- 请求ID追踪:通过`RequestIdMiddleware`为每个请求分配唯一ID
- 错误日志记录:通过`ErrorLoggingMiddleware`记录所有异常
- CORS未配置(若需跨域调用需添加CORSMiddleware)

**当前代码未体现的开放能力**:
- 无API key认证机制(所有端点公开访问)
- 无用户权限管理(角色区分)

#### 3.4.2 定制化支持

**模板定制**:
- 支持用户自定义PPTX模板文件与descriptor JSON
- 无需修改代码,仅需添加文件到 `data/templates/` 并更新catalog.json
- 占位符类型可扩展:添加新图表类型仅需修改3个文件(见文档附录"新增图表类型快速指南")

**数据格式定制**:
- TenantInput支持任意JSON结构,通过dotted path访问嵌套字段
- Excel数据提取脚本需用户自定义(openpyxl单元格映射规则)

**AI指令定制**:
- 每个占位符的ai_instruction可单独配置
- 支持模板级别的system prompt定制(修改LLMOrchestratorV2._build_system_prompt)

**当前代码未体现的定制能力**:
- 无Web UI配置模板(需手动编辑JSON)
- 无可视化模板编辑器

---

### 3.5 安全性

#### 3.5.1 身份认证与鉴权

**当前实现状态**: 无身份认证机制,所有API端点公开访问

**建议增强**:
- 添加API key认证:`Authorization: Bearer {api_key}` header
- 使用FastAPI Security模块实现OAuth2 / JWT认证
- 区分角色:
  - 普通用户:可调用 `/generate`, `/upload-excel`, `/rewrite`, `/preview`, `/download`
  - 管理员:可调用 `/cleanup`, 查看审计日志

#### 3.5.2 数据安全

**敏感信息保护(已实现)**:
- OpenAI API key存储在.env文件中,不硬编码
- 环境变量通过dotenv加载,不提交到版本控制

**输入数据安全(已实现)**:
- Excel上传:
  - 仅接受.xlsx格式,拒绝.xlsm(宏Excel)
  - MIME类型双重验证
  - openpyxl只读模式,data_only=True,不执行宏或公式
  - 文件大小限制10MB
- JSON输入:无特殊校验,假设来自可信来源

**当前代码未体现的安全机制**:
- 无输入JSON schema校验(建议使用pydantic验证TenantInput结构)
- 无XSS防护(若Web UI展示用户输入的文本,需HTML转义)
- 无CSRF保护(若有Web表单需添加CSRF token)

#### 3.5.3 API防护

**当前实现状态**:
- LLM并发限流(Semaphore):防止过多请求导致OpenAI API rate limit
- 文件锁:防止并发写入冲突

**当前代码未体现的防护机制**:
- 无速率限制(rate limiting):单个用户可无限制调用API
- 无请求大小限制:可能被恶意大JSON payload攻击
- 无IP白名单/黑名单

**建议增强**:
- 使用slowapi或FastAPI-Limiter添加速率限制:`@limiter.limit("10/minute")`
- 配置FastAPI max_request_size限制请求体大小
- 添加IP白名单中间件(生产环境)

#### 3.5.4 异常与攻击防护

**已实现的防护**:
- 异常捕获:所有API端点使用try-except,返回HTTP 4xx/5xx错误
- 文件路径校验:session_id通过uuid生成,防止路径遍历攻击
- 超时保护:LLM API调用有重试次数限制,文件锁有超时限制

**潜在攻击面**:
1. **文件上传攻击**: 恶意Excel文件可能利用openpyxl解析漏洞(建议升级openpyxl到最新版本)
2. **Prompt注入**: 恶意输入数据可能包含特殊字符,影响LLM生成内容(当前未防护,建议sanitize输入)
3. **DoS攻击**: 大量并发请求可能耗尽服务器资源(建议添加rate limiting与请求队列)

---

### 3.6 可测试性

#### 3.6.1 单元测试支持

**当前代码的可测试性**:
- 模块化设计:ReportService, LLMOrchestratorV2, PPTGeneratorV2等类职责清晰,易于Mock
- 依赖注入:LLMOrchestratorV2接受TemplateRepository参数,可注入Mock对象
- Mock模式:支持use_mock=true,跳过LLM调用,填充fallback内容

**建议的单元测试用例**:
1. **LLMOrchestratorV2测试**:
   - 测试_extract_data_placeholders():验证数据提取逻辑(dotted path, format, transform)
   - 测试_extract_chart_data():验证图表数据转换(bar_chart, pie_chart, P11_bar等)
   - 测试_parse_llm_response():验证JSON解析与错误处理
   - Mock OpenAI client,测试重试逻辑

2. **PPTGeneratorV2测试**:
   - 测试_render_bar_chart():验证图表创建与样式
   - 测试_replace_tokens_in_shape():验证占位符替换逻辑
   - 使用临时PPTX模板,验证render()输出文件

3. **ReportService测试**:
   - Mock各子模块,测试generate()端到端流程
   - 测试异常处理:InputNotFoundError, SlideSpecNotFoundError等

#### 3.6.2 集成测试支持

**已有的测试工具**:
- test_charts.py:测试图表生成功能
- test_concurrency.py:并发安全测试(5用户同时生成报告)

**建议的集成测试场景**:
1. **端到端测试**:
   - 上传Excel → 生成报告 → 预览 → 下载 → 改写 → 重新预览
   - 验证文件存在性、内容正确性

2. **并发测试**:
   - 10用户同时上传Excel并生成报告
   - 验证session隔离、无文件冲突、响应时间

3. **故障注入测试**:
   - Mock OpenAI API返回超时/错误
   - Mock LibreOffice命令失败
   - 验证降级逻辑与错误提示


---

## 4. 其他

### 4.1 部署指南

**系统依赖**:
- Python 3.9+
- LibreOffice (用于预览图生成)
- pip packages:见 `requirements.txt`

**部署步骤**:
1. 克隆代码库:`git clone ...`
2. 安装依赖:`cd mss_ai_ppt_sample_assets/backend && pip install -r requirements.txt`
3. 配置环境变量:在项目根目录创建`.env`文件:
```bash
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.openai.com/v1  # 可选
OPENAI_MODEL=gpt-4o-mini
ENABLE_LLM=true
DEFAULT_LOCALE=zh-CN
```
4. 启动服务:`python app.py`(或 `uvicorn mss_ai_ppt_sample_assets.backend.app:app --host 0.0.0.0 --port 8000`)
5. 访问Web UI:`http://localhost:8000/ui`
6. 访问API文档:`http://localhost:8000/docs`

**生产环境建议**:
- 使用Gunicorn/Uvicorn多worker模式
- 配置Nginx反向代理(处理静态文件、SSL termination)
- 使用systemd/supervisor管理进程
- 配置日志轮转(logrotate)
- 定期清理会话目录(cron job调用 `/cleanup`)

### 4.2 测试验收场景

#### 4.2.1 正常场景

**场景1: Excel上传 → 报告生成 → 预览 → 下载(完整流程)**
1. 准备一个符合格式的Excel文件(security_report_2025-12.xlsx)
2. 调用 `POST /upload-excel`,上传Excel文件
3. 验证响应:status=success, session_id非空, preview字段包含客户名称与告警总数
4. 调用 `POST /generate`,使用返回的session_id,选择template_id="mss_executive_v2", use_mock=false
5. 验证响应:job_id格式正确(session_id:template_id), report_path存在
6. 调用 `GET /preview?job_id={job_id}`
7. 验证响应:images数组包含8个PNG URL
8. 浏览器访问PNG URL,验证图片可正常显示
9. 调用 `GET /download?job_id={job_id}`
10. 验证返回PPTX文件,使用PowerPoint打开,检查内容:
    - 客户名称、报告周期正确
    - 图表数据与Excel一致
    - AI生成的威胁分析语言流畅

**场景2: 内容改写(Rewrite)**
1. 基于场景1生成的报告,记录job_id
2. 查看 `outputs/sessions/{session_id}/slidespec_{template_id}.json`,找到slide_key="summary"的幻灯片
3. 调用 `POST /rewrite`,提交new_content:
```json
{
  "job_id": "...",
  "slide_key": "summary",
  "new_content": {
    "THREAT_ANALYSIS": "自定义的威胁分析内容"
  }
}
```
4. 验证响应:report_path更新
5. 下载新的PPTX,打开"summary"幻灯片,验证THREAT_ANALYSIS内容已替换

**场景3: 并发生成(5用户)**
1. 准备5个不同的Excel文件(或使用同一Excel,session_id会自动区分)
2. 使用脚本并行调用 `POST /generate` (5个请求)
3. 验证:
   - 5个请求全部成功(status=success)
   - session_id互不冲突
   - 生成的PPTX文件存储在不同目录
   - 响应时间差异<2秒(说明无明显阻塞)

**场景4: Mock模式降级**
1. 设置环境变量 `ENABLE_LLM=false` 或 请求参数 `use_mock=true`
2. 调用 `POST /generate`
3. 验证:
   - 报告生成成功
   - AI占位符内容为fallback文本(如"[AI生成内容占位]")
   - 响应时间<10秒(无LLM调用)

#### 4.2.2 异常场景

**场景1: OpenAI API超时**
1. 临时修改OpenAI API endpoint为无效地址(模拟网络故障)
2. 调用 `POST /generate` (use_mock=false)
3. 验证:
   - 响应返回HTTP 500错误
   - 错误消息包含:"Failed after 4 attempts" 或 "建议切换到mock模式"
   - 审计日志中记录失败事件

**场景2: Excel格式错误**
1. 上传一个.csv文件(重命名为.xlsx)
2. 调用 `POST /upload-excel`
3. 验证:
   - 响应返回HTTP 400错误
   - 错误消息:"仅支持.xlsx格式"

**场景3: 文件锁冲突(模拟)**
1. 手动创建一个文件锁:`touch outputs/sessions/{session_id}/report_xxx.pptx.lock`
2. 调用 `POST /generate` (使用相同session_id)
3. 验证:
   - 若在60秒内,响应返回超时错误
   - 若超过60秒,锁文件自动删除(需确认FileLock.timeout=60生效)

**场景4: 数字校验失败**
1. 修改输入JSON,将 `alerts.total` 从12455改为99999
2. 调用 `POST /generate` (use_mock=false)
3. 验证:
   - 报告生成成功(不会中断)
   - 响应中warnings数组包含:"ALERT_TOTAL: expected 99999, got 12455"(若AI引用了真实数据)
   - 审计日志中记录validation_warning_v2事件

**场景5: SlideSpec不存在**
1. 调用 `POST /rewrite`,提交一个不存在的job_id
2. 验证:
   - 响应返回HTTP 404错误
   - 错误消息:"Slidespec for session ... not found. Please generate first."

**场景6: 预览生成失败(LibreOffice未安装)**
1. 卸载LibreOffice或临时修改PATH移除soffice
2. 调用 `GET /preview?job_id={job_id}`
3. 验证:
   - 响应返回HTTP 500错误
   - 错误消息:"LibreOffice not found. Please install LibreOffice."

---

### 4.3 系统限制与已知问题

**限制条件**:
1. **单机部署限制**: 当前使用本地文件锁,不支持多实例水平扩展。若需分布式部署,需改造为Redis分布式锁。
2. **Excel格式依赖**: Excel数据提取逻辑硬编码单元格位置(如A2=客户名称),不同格式的Excel需修改提取脚本。
3. **模板与数据强耦合**: 模板descriptor中的source字段与输入JSON结构强绑定,修改输入结构需同步更新模板。
4. **AI生成不稳定**: LLM生成内容受模型版本、prompt影响,可能出现格式错误或数据引用不准确。

**已知问题(基于代码分析)**:
1. **API认证缺失**: 所有端点公开访问,存在安全风险(建议添加API key或OAuth2)。仅有IP速率限制(30秒/请求)作为基础防护。
2. **分布式部署限制**: 文件锁为本地锁,不支持多实例水平扩展(需升级为Redis分布式锁)。
3. **任务重试机制有限**: 最多重试3次,指数退避最长8秒。对于长时间API故障,可能导致任务最终失败。
4. **预览图清理依赖启动触发**: 仅在生成新预览时清理旧图,若长时间无预览请求,旧图会积累(建议添加定时清理任务)。
5. **文件锁陈旧锁处理**: 虽启动时自动清理,但运行时进程崩溃可能残留锁文件,需等待下次重启或手动清理。
6. **任务状态恢复有限**: 服务器重启后,processing状态的任务不会自动恢复(需JobManager实现恢复逻辑,当前未完全实现)。
7. **i18n国际化未完全实现**: 虽挂载了i18n文件,但后端API响应仍为硬编码中文,未支持多语言切换。

**相比文档初版已修复的问题**:
- ✅ 应用日志已持久化(10MB轮转 x 5)
- ✅ 会话清理已实现(启动时+手动API)
- ✅ 陈旧锁自动清理(启动时,5分钟超时)
- ✅ 预览图自动清理(生成时清理7天前,可配置)

---

### 4.4 术语表

| 术语 | 定义 |
|------|------|
| MSS | Managed Security Service,托管安全服务 |
| TenantInput | 输入数据的封装类,支持通过dotted path访问嵌套字段 |
| SlideSpecV2 | V2模板的中间数据结构,包含幻灯片编号、slide_key与占位符字典 |
| TemplateDescriptorV2 | V2模板描述文件,定义幻灯片结构与占位符规则 |
| PlaceholderDefinition | 占位符定义,包含token名称、类型、是否AI生成、ai_instruction等字段 |
| session_id | 唯一会话标识符,格式为`{uuid8}_{timestamp}`,用于隔离并发请求的文件存储 |
| job_id | 任务标识符,格式为`{session_id}:{template_id}`,用于关联生成的报告与SlideSpec |
| ai_generate | 占位符属性,true表示需LLM生成内容,false表示从输入数据提取 |
| source | 占位符属性,指定从输入数据中提取值的路径(dotted notation,如"alerts.total") |
| ai_instruction | 占位符属性,指导LLM生成内容的提示词 |
| FileLock | 跨平台文件锁实现,使用os.O_CREAT\|os.O_EXCL原子性创建锁文件 |
| LLM并发限流 | 使用asyncio.Semaphore限制同时调用OpenAI API的请求数(默认3个) |
| 智能分批 | LLMOrchestratorV2根据prompt token数估算,将幻灯片分成多批生成,避免单次API超时 |
| 占位符定位 | P11_bar等专用图表类型使用的渲染模式,通过{{TOKEN}}文本框位置查找最近的图表对象 |
| JobManager | 任务管理器,负责任务状态持久化、自动重试、任务清理 |
| JobStore | 任务存储,将任务状态保存到outputs/jobs/states/目录 |
| request_id | 请求唯一标识符,由RequestIdMiddleware生成,用于跨模块关联日志 |
| IP速率限制 | 基于客户端IP的简单速率限制,默认30秒/请求,防止滥用 |
| 异步任务队列 | 后台异步处理报告生成,立即返回job_id,通过WebSocket推送进度 |

---

## 5. 附录

### 5.1 架构决策记录(ADR)

**ADR-001: 为什么选择V2模板架构?**
- **背景**: V1架构使用预处理后的DataPrepResult,需维护复杂的render_map路径
- **决策**: V2架构简化为占位符直驱动,SlideSpecV2.placeholders直接映射token→value
- **优势**: 降低复杂度,模板定义更直观,易于扩展新占位符类型
- **劣势**: 与V1不兼容,历史模板需迁移

**ADR-002: 为什么使用本地文件锁而非数据库?**
- **背景**: 需保证并发请求的文件写入原子性
- **决策**: 使用os.O_CREAT|os.O_EXCL创建锁文件,跨平台兼容且无外部依赖
- **优势**: 实现简单,性能高,无需部署Redis/数据库
- **劣势**: 不支持分布式部署,陈旧锁需手动清理

**ADR-003: 为什么使用智能分批而非固定幻灯片数分批?**
- **背景**: 不同幻灯片的AI占位符数量差异大,固定分批可能导致某批超时
- **决策**: 根据prompt token数估算(字符数/2),动态调整每批包含的幻灯片数
- **优势**: 避免API超时,提高生成成功率
- **劣势**: token估算不精确(实际token数受tokenizer影响)

### 5.2 扩展指南

**如何添加新的图表类型(如P17_scatter散点图)?**

参考文档"excel-to-ppt-workflow.md"附录部分,需修改4个文件:
1. models/templates.py:在PlaceholderDefinition的type枚举中添加"P17_scatter"
2. modules/ppt_generator.py:
   - 在_chart_renderers字典中注册渲染函数
   - 实现_render_p17_scatter()方法(使用XL_CHART_TYPE.XY_SCATTER)
3. modules/llm_orchestrator.py:
   - 在_extract_data_placeholders()中添加P17_scatter到类型列表
   - (可选)在_extract_chart_data()中添加特殊数据格式处理
4. data/templates/{template_id}_descriptor.json:添加占位符定义

**如何自定义AI生成的system prompt?**

修改 `modules/llm_orchestrator.py` 中的 `_build_system_prompt()` 方法,调整角色描述、关键要求、语言风格等。

**如何集成新的LLM提供商(如Claude, Gemini)?**

当前系统使用OpenAI兼容接口,只需设置环境变量:
```bash
OPENAI_BASE_URL=https://api.anthropic.com/v1  # Claude示例
OPENAI_API_KEY=sk-ant-...
OPENAI_MODEL=claude-3-5-sonnet-20241022
```
若API格式不兼容,需修改 `LLMOrchestratorV2._call_openai_with_retry()` 适配新接口。

---

## 6. 文档版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|---------|
| 1.0 | 2026-01-30 | Claude AI | 初始版本,基于代码分析与excel-to-ppt-workflow.md编写 |
| 1.1 | 2026-02-03 | Claude AI | 重大更新:更新为实际代码架构,添加异步任务队列、JobManager、日志轮转、IP速率限制等新增功能 |

---

**文档说明**:
本文档基于以下文件分析生成:
- app.py (主应用入口,路由注册)
- services/report_service.py (核心服务逻辑)
- services/job_manager.py (任务管理)
- modules/llm_orchestrator.py (AI生成与数据提取)
- modules/ppt_generator.py (图表与表格渲染)
- modules/job_store.py (任务状态持久化)
- logging_config.py (日志配置)
- middleware.py (请求ID、错误日志中间件)
- routers/v1/*.py (RESTful API端点)
- config.py (系统配置)

**v1.1版本主要变更**:
1. **API路由结构**: 从扁平路径更新为`/api/v1/*` RESTful结构
2. **异步处理**: 从同步阻塞更新为异步任务队列模式
3. **任务管理**: 新增JobManager、JobStore、任务状态持久化
4. **日志系统**: 新增应用日志轮转、请求ID追踪、错误日志中间件
5. **安全增强**: 新增IP速率限制(30秒/请求)
6. **自动化运维**: 新增启动时自动清理(陈旧锁、旧任务、旧会话、旧预览图)
7. **国际化**: 新增i18n多语言支持(前端)
8. **健康检查**: 增强健康检查端点,添加LLM并发状态、磁盘空间监控

**已验证符合当前代码的功能**:
- ✅ 核心架构(V2模板、FastAPI后端)
- ✅ 并发控制(Semaphore、FileLock、IP速率限制)
- ✅ 任务管理(JobManager、状态持久化、自动重试)
- ✅ 日志系统(轮转、请求ID追踪、审计日志)
- ✅ API端点(完整的RESTful v1路由)
- ✅ 自动化运维(启动清理、预览图清理)

**当前代码未实现但文档中保留的建议**:
- API key认证(建议实现)
- Redis分布式锁(分布式部署需要)
- Prometheus metrics端点
- OpenTelemetry分布式追踪
